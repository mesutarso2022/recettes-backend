'use strict';

/**
 * dgrad-administratif service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dgrad-administratif.dgrad-administratif');
